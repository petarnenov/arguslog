# Railway config for the argus-ingest service.
# Built with Nixpacks (auto-detects Java 21 + Gradle).
# Provisioned in P5 alongside the other services.

[build]
builder = "NIXPACKS"
buildCommand = "./gradlew :services:ingest:bootJar -x test --no-daemon"

[deploy]
startCommand = "java $JAVA_TOOL_OPTIONS -jar services/ingest/build/libs/ingest-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/actuator/health/readiness"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
numReplicas = 1
