name: arguslog-local

services:
  postgres:
    image: timescale/timescaledb:latest-pg16
    container_name: arguslog-postgres
    environment:
      POSTGRES_USER: arguslog
      POSTGRES_PASSWORD: arguslog
      POSTGRES_DB: arguslog
    ports:
      - '5432:5432'
    volumes:
      - arguslog-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U arguslog -d arguslog']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: arguslog-redis
    command: ['redis-server', '--appendonly', 'yes']
    ports:
      - '6379:6379'
    volumes:
      - arguslog-redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10

  keycloak:
    image: quay.io/keycloak/keycloak:25.0
    container_name: arguslog-keycloak
    command:
      - start-dev
      - --import-realm
      - --hostname-strict=false
      - --health-enabled=true
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_HTTP_PORT: '8180'
    ports:
      - '8180:8180'
    volumes:
      - ../../services/keycloak/realm:/opt/keycloak/data/import:ro
      # Custom Arguslog login theme — the Dockerfile bakes it into the prod image, but dev
      # uses the upstream KC image so we mount the directory directly. Referenced from the
      # realm via "loginTheme": "arguslog".
      - ../../services/keycloak/themes/arguslog:/opt/keycloak/themes/arguslog:ro
    healthcheck:
      test:
        [
          'CMD-SHELL',
          "exec 3<>/dev/tcp/127.0.0.1/9000 && echo -e 'GET /health/ready HTTP/1.0\\r\\n\\r\\n' >&3 && cat <&3 | grep -q '200 OK'",
        ]
      interval: 10s
      timeout: 5s
      retries: 20

  minio:
    image: minio/minio:latest
    container_name: arguslog-minio
    command: ['server', '/data', '--console-address', ':9001']
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - arguslog-minio-data:/data
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 10s
      timeout: 5s
      retries: 10

  minio-bucket-init:
    image: minio/mc:latest
    container_name: arguslog-minio-init
    profiles: ['init']
    depends_on:
      minio:
        condition: service_healthy
    entrypoint:
      - /bin/sh
      - -c
      - |
        mc alias set local http://minio:9000 minioadmin minioadmin &&
        (mc mb local/arguslog-sourcemaps || true) &&
        (mc mb local/arguslog-attachments || true) &&
        echo "MinIO buckets ready"

  mailhog:
    image: mailhog/mailhog:latest
    container_name: arguslog-mailhog
    ports:
      - '1025:1025' # SMTP
      - '8025:8025' # Web UI

volumes:
  arguslog-postgres-data:
  arguslog-redis-data:
  arguslog-minio-data:
