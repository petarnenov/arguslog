node_modules
dist
build
.turbo
coverage
.gradle
**/build
**/target
pnpm-lock.yaml
# Pact JSON files are generated by @pact-foundation/pact and must stay
# byte-identical between consumer regeneration and the committed copy
# (CI compares with `git diff --exit-code pacts/`). Don't reformat.
pacts/
# OpenAPI spec is generated by springdoc and matched byte-for-byte by
# OpenApiContractTest. Don't reformat or the contract test fails.
services/api/openapi.json
# Python tooling state — virtualenvs, pytest/ruff caches, build artifacts.
**/.venv
**/.pytest_cache
**/.ruff_cache
**/__pycache__
**/*.egg-info
python-sdk/dist
