node_modules
dist
build
.turbo
coverage
.gradle
**/build
**/target
pnpm-lock.yaml
# Pact JSON files are generated by @pact-foundation/pact and must stay
# byte-identical between consumer regeneration and the committed copy
# (CI compares with `git diff --exit-code pacts/`). Don't reformat.
pacts/
