# Node
node_modules/
.pnpm-store/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/
# IntelliJ module output at the repo root only — must NOT match legitimate
# package paths like adapter/out/ in our hexagonal layout.
/out/
.turbo/
.vite/
coverage/
*.tsbuildinfo

# Java / Gradle
.gradle/
build/
!gradle-wrapper.jar
!**/src/**/build/
bin/
*.class
*.jar
!gradle/wrapper/*.jar
hs_err_pid*
replay_pid*

# IDE
.idea/
.vscode/
!.vscode/extensions.json
!.vscode/settings.recommended.json
*.iml
*.ipr
*.iws
.project
.classpath
.settings/

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.local
.env.*.local
*.pem
*.key
secrets/

# Docker volumes (local dev)
infra/docker/volumes/

# Keycloak realm — generated by services/keycloak/render-realm.sh from
# realm.template.json with the active DEV_HOST substituted. The template is
# the canonical version in git; never edit the generated file by hand.
services/keycloak/realm/arguslog-realm.json

# Test artifacts
playwright-report/
test-results/
.playwright/

# Misc
*.tmp
*.bak
